/**
 * 파일 역할: 주식 종목 심볼 context configuration
 * 작성자: Jason (parixscm)
 * 최근 업데이트: 2023.04.08.
 */

import { createContext } from "react";

const StockContext = createContext();

export default StockContext;
