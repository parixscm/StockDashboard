/**
 * 파일 역할: 다크모드 context configuration
 * 작성자: Jason (parixscm)
 * 최근 업데이트: 2023.04.07.
 */

import { createContext } from "react";

const ThemeContext = createContext();

export default ThemeContext;
